# Prompt Overrides

Workspace prompt overrides live here.

Dream uses the bundled memory consolidation prompt by default. To customize it for this workspace, run:

```text
/dream-prompt init
```

That creates `prompts/dream.md`. Edit that file to change how Dream consolidates memory. Delete or empty it to return to the bundled default.
